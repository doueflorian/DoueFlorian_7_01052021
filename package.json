{
    "name": "groupomania",
    "scripts": {
        "install": " concurrently \"cd frontend && npm install\" \"cd backend && npm install\" ",
        "start": " concurrently \"cd frontend && npm run serve\" \"cd backend && nodemon run serve\" "
    },
    "dependencies": {
        "concurrently": "^6.3.0"
    },
    "devDependencies": {}
}
